spring:
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password:

  flyway:
    enabled: true

  jpa:
    hibernate:
      ddl-auto: validate

  sql:
    init:
      mode: always